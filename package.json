{
  "name": "rainbow-portal",
  "version": "1.0.0",
  "description": "Rainbow by ALE - Sales Portal",
  "scripts": {
    "start": "npx serve -s . -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
